<% unless current_user?(@user) %> <!--如果不是显示本用户-->
	<div id="follow_form">
		<% if current_user.following?(@user) %><!--如果已经关注了本用户-->
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>
